---
---

--- Before Day-2 ---
I already knew ...
--- 

## Day-2 Checklist

- [k ] I understand what `PATH` is and why commands like `python` work without full paths
- [k ] I can navigate the filesystem without clicking — using `cd`, `ls`, and `pwd` only
- [k ] I can read, search, and inspect files using `cat`, `head`, `tail`, `grep`, and `wc`
- [k ] I can edit a file using `nano` (open, edit, save, exit)
- [k ] I understand pipes (`|`) and redirection (`>`, `>>`, `2>`) and can chain commands
- [ ] I can set an environment variable in `.bashrc` and apply it with `source ~/.bashrc`
- [ ] I know the difference between `export VAR=value` (available to child processes) and just `VAR=value` (shell-local)

--- After Day-2 ---
I learned these things as well, apart from the checklist I learned about setting environment variables
in .bashrc using export VAR=value and applying changes with source ~/.bashrc so they persist across sessions.
I also learned the difference between export VAR=value, which makes a variable available to child processes,
and VAR=value, which is only available within the current shell session.
---

--- Feedback (Suggestions for the TDS Team) ---
This is my feedback The Day-2 content was well structured.
It would be helpful to have a few more practice exercises on .bashrc and environment variables,
as those concepts were new to me. A short cheat sheet summarising the most used commands covered each day would also be a great addition.
---

---
---

You can write your personal notes here; they will not be parsed and are for your own reference.
